import { CreateBookingResponseModel } from './create-booking-response-model';

describe ('CreateBookingResponseModel', () => {
  it ('should create an instance', () => {
    expect (new CreateBookingResponseModel ()).toBeTruthy ();
  });
});
