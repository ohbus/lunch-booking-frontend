import { NewPasswordRequest } from './new-password-request';

describe('NewPasswordRequest', () => {
  it('should create an instance', () => {
    expect(new NewPasswordRequest()).toBeTruthy();
  });
});
