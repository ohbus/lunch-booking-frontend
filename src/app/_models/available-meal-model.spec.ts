import { AvailableMealModel } from './available-meal-model';

describe('AvailableMealModel', () => {
  it('should create an instance', () => {
    expect(new AvailableMealModel()).toBeTruthy();
  });
});
